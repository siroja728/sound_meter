import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    scrollContent: {
        flex:1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    currentLevel: {
        fontSize: 40,
        textAlign: 'center',
    },
    textCenter: {
        textAlign: 'center'
    }
});
